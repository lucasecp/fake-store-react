export interface FilterI {
  category: string;
  name: string;
}
export interface ProductI {
    id: number
  title: string;
  image: string;
  category: string;
  price: string;
  description: string
}
